// Scaffold only — Autoverse operations portal: pipeline board, catalogue, commercial, ops, RBAC.
// Content arrives with its Phase 1 spec; nothing is built here during ENGINE-MIGRATION.
export default function Page() {
  return <main>Admin</main>;
}
