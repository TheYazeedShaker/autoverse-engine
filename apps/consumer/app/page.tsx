// Scaffold only — ONE app, three entitlement-gated surfaces: Showroom, Brochure, Configurator.
// Content arrives with its Phase 1 spec; nothing is built here during ENGINE-MIGRATION.
export default function Page() {
  return <main>Consumer</main>;
}
