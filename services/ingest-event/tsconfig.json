{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "noEmit": true },
  // index.ts is Deno (Deno.serve) and is typechecked by CI's deno job (services/deno.json), not tsc
  // The rules it applies live in ingest.ts, which IS typechecked and unit tested here.
  "include": ["ingest.ts", "ingest.test.ts"]
}
