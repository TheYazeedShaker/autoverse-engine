{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "noEmit": true },
  // index.ts is Deno (Deno.serve, npm: specifiers) and is typechecked by the Supabase CLI at deploy
  // time. The rules it applies live in ingest.ts, which IS typechecked and unit tested here.
  "include": ["ingest.ts", "ingest.test.ts"]
}
